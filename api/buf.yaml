---

version: v2

modules:
  - path: proto

lint:
  use:
    - STANDARD
  # Always exclude third-party proto files from linting.
  ignore:
    - proto/third_party/buf/validate/validate.proto

breaking:
  use:
    - FILE
