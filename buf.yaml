version: v2

deps:
  - buf.build/bufbuild/protovalidate:v0.13.3

modules:
  - path: api/proto

lint:
  use:
    - STANDARD
  ignore:
    - proto/google/type/datetime.proto

breaking:
  use:
    - FILE
