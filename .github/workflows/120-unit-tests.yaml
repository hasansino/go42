---

name: unit-tests

# Unit tests.

on:
  workflow_call:
    inputs:
      runs_on:
        required: true
        type: string
      continue_on_error:
        required: true
        type: boolean
      timeout_minutes:
        required: false
        type: number
        default: 10

permissions: {}

jobs:
  unit-tests:
    name: unit-tests
    runs-on: ${{ inputs.runs_on }}
    continue-on-error: ${{ inputs.continue_on_error }}
    timeout-minutes: ${{ inputs.timeout_minutes }}
    permissions:
      contents: read
    # ----
    steps:
      - name: 'Checkout repository'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: 'Setup Go'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: 'go.mod'
      - name: 'Download Go modules'
        run: go mod download
      - name: 'Run unit tests with race detector'
        run: |-
          go list ./... | grep -v '/tests/' | \
            xargs go test -count=1 -v -race -covermode=atomic -coverprofile=coverage.out
        env:
          CGO_ENABLED: 1 # needed for -race
      - name: 'Check test coverage'
        uses: vladopajic/go-test-coverage@f94bcf0d6b9fa5fb8b783830b22648f6c17475e2 # v2.19.0
        with:
          config: ./etc/testcoverage.yaml
      - name: 'Upload coverage profile'
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: code-coverage
          path: coverage.out
          if-no-files-found: error

  coverage-report:
    name: coverage-report
    if: github.event_name == 'pull_request'
    runs-on: ${{ inputs.runs_on }}
    continue-on-error: true
    timeout-minutes: ${{ inputs.timeout_minutes }}
    permissions:
      actions: read
      contents: read
      pull-requests: write
    needs:
      - unit-tests
    # ----
    steps:
      - name: 'Generate coverage report'
        id: coverage_report
        uses: fgrosse/go-coverage-report@e432de98ee94a276e8f666d25bfd76347665f75b # v1.3.1
        with:
          version: v1.3.1
          sha256sum: bce196efd0d22bbdcfa2ab45dc38ad40e96acc5802356b48be358a43958674a0
          coverage-artifact-name: code-coverage
          coverage-file-name: coverage.out
          root-package: github.com/hasansino/go42
          trim: github.com/hasansino/go42
          exclude: '(^|/)(api/gen|mocks)/'
          skip-comment: >-
            ${{ github.event.pull_request.head.repo.full_name != github.repository || github.actor == 'dependabot[bot]' }}
      - name: 'Add coverage report to job summary'
        if: steps.coverage_report.outputs.coverage_report != ''
        run: printf '%s\n' "$COVERAGE_REPORT" >> "$GITHUB_STEP_SUMMARY"
        env:
          COVERAGE_REPORT: ${{ steps.coverage_report.outputs.coverage_report }}
