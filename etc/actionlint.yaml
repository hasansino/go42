---

config-variables:
  - RELEASE_APP_ID
  - RELEASE_APP_NAME
  # ----
  - CLAUDE_ENABLE
  - CLAUDE_MODEL
  - CLAUDE_APP_ID
  - CLAUDE_APP_NAME
  - CLAUDE_TIMEOUT
  - CLAUDE_MAX_TURNS
  # ----
  - GEMINI_ENABLE
  - GEMINI_MODEL
  - GEMINI_APP_ID
  - GEMINI_APP_NAME
  - GEMINI_TIMEOUT
  - GEMINI_MAX_TURNS
  # ----
  - SUMMARY_ENABLE
  - SUMMARY_MODEL

paths:
  .github/workflows/210-github-pages.yaml:
    ignore:
      # GitHub supports concurrency queueing, but actionlint 1.7.12 does not yet
      # recognize the `queue` key: https://github.com/rhysd/actionlint/issues/680
      - 'unexpected key "queue" for "concurrency" section.+'
  .github/workflows/150-resilience-tests.yaml:
    ignore:
      # actionlint does not support service container commands yet
      - 'unexpected key "command" for "services" section.+'
  .github/workflows/*.{yml,yaml}:
    ignore:
      # actionlint does not support the `models` permission scope as of now
      - 'unknown permission scope \"models\".+'
